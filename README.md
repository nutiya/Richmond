# Blue Horizon School Website

Responsive static school website built with React, React Router, and modern CSS.

## Run locally

```bash
npm install
npm run dev
```

## Structure

- `src/components` reusable UI (`Navbar`, `Footer`, `Card`)
- `src/pages` page-level components
- `src/styles/global.css` shared theme, layout, and animations
